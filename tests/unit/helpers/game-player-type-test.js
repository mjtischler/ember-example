
import { gamePlayerType } from 'psnow-ember-example/helpers/game-player-type';
import { module, test } from 'qunit';

module('Unit | Helper | game player type');

// Replace this with your real tests.
test('it works', function(assert) {
    let result = gamePlayerType([42]);
    assert.ok(result);
});
